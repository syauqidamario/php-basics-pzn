?<?php
      $name = "Hiroki";
      $name = NULL;

      //NULL itu representasi variabel tanpa nilai

      $age = null;

      $isNull = is_null($name);
      var_dump($isNull);

      unset($value);
      var_dump(isset($value));

      $value = "Hiroki";
      var_dump($value);
